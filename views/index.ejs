<%- include header.ejs %>

<% if (!user) { %>
	<div id="container" class="container">
	<div class="hero-unit">
		<h1>欢迎来到 guof's blog</h1>
		<p>Microblog 是一个基于 Node.js 的微博系统。</p>
		<p>
			<a class="btn btn-primary btn-large" href="/login">登录</a>
			<a class="btn btn-large" href="/reg">立即注册</a>
		</p>
	</div>
	</div>
<% } else { %>
	<%- include say.ejs %>
<% } %>
	<%- include post.ejs %>

<%- include footer.ejs %>